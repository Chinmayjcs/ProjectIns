# ShadowPass — Password Strength Analyzer (MERN)

Simple MERN mini-project for Information & Network Security (mini 5-mark project).

Features:
- Check password strength with a 0-100 score and labeled feedback.
- Generate secure random passwords (min length 6).
- Optional MongoDB logging of masked checks.

## Quick start 
1. Clone / copy project folder `password-analyzer-mern`.
2. Start backend:
   ```bash
   cd server
   npm install
   npm start
2. Start Frontend:
   ```bash
   cd client
   npm install
   npm start
